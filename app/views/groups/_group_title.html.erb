
	<a href="javascript:void(0);" class="slideHandle <% if group.has_children_or_entries? %>toggle<% end %>"><%= group.title %></a>  

	<!--
	<em>10 Projects</em>
	-->
	
	<% if group.allows_write_access_for?(current_user) %>
	  <a href="<%= edit_group_url(group) %>" class="edit">edit</a>
	<% end %>

