<li>
  <%= permission.group.title_with_parents %>
  <%= permission.mode %>
  [<%= link_to_remote "x", :url => permission_url(permission, :user_id => @user.id), 
    :method => :delete, :update => "permissions" %>]
</li>
